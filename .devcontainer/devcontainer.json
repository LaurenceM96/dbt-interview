{
  "name": "dbt Analytics Engineer Interview",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "postCreateCommand": "cd dbt_project && dbt deps && dbt seed && dbt build",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "innoverio.vscode-dbt-power-user",
        "mechatroner.rainbow-csv"
      ]
    }
  }
}